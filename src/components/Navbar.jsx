import React from 'react';
import { Link } from 'react-router-dom';

export const Navbar = () => {
    return <div>
        <nav>
            {/* <button>LOG IN</button> */}
        </nav>
    </div>;
};
